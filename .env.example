# Copy to .env and fill in once the Google backend is deployed.
# Each should be a Google Apps Script Web App URL (or equivalent) that accepts
# a POST with a JSON body and returns a 2xx response. See src/lib/submitForm.ts
# for the exact payload shape sent to each endpoint.

VITE_INQUIRY_ENDPOINT=
VITE_CONSULTANT_APPLICATION_ENDPOINT=
VITE_REVIEW_ENDPOINT=
